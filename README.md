# Stayora

Initial workspace for Stayora — Full-Stack Vacation Rental Marketplace.

This repository contains two main folders: `frontend/` and `backend/`.

See `frontend/README.md` and `backend/README.md` for local setup instructions.
